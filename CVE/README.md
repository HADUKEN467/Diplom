
'''
НАСТРОЙКИ:
http://localhost:8000/docs - забиваем в браузер для открытия Swagger и взаимодействием API(Ручная загрузка zip-файла) http://localhost:8081

http://localhost:8001/docs - забиваем в браузер для открытия Swagger и взаимодействием API(Обновление через ссылку) http://localhost:8082

==localhost:8081(8082)== - база данных Mongo Express с веб-интерфейсом для просмотра содержимых данных
Пользователь: admin
Пароль: 123

Необходимо полностью менять адрес базы данных в коде, что не очень удобно.

Для ручной загрузки:
	Для ручной загрузки необходимо закинуть в папку "./Manual_download/download_zipfile (с двумя нижними подчеркиваниями в начале и конце названия)" необходимый архив и указать его имя в конфигурации, после чего можно вручную установить или обновить файл
'''

Можно загрузить репозиторий через URL на mongodb, но конкретно CVE потестить не могу из-за большого объёма. Программа именно клонит, а не пуллит. Пулл и корректное сохранение будет в дальнейшем. Создам тестовый репозиторий и буду экспериментировать. Как доделаю нормальный клон репозитория, займусь докером и дальше авто обновлением.

Upd 1:
Добавил контейнер. Следующий шаг: создание тестового репозитория с CVE файлами допустим за 2025 год и улучшение корректного сохранения в NoSQL бд.

Upd 2:
Создал открытый тестовый репозиторий.
Добавил Mongo Express в контейнер. Теперь можно клонировать официальный репозиторий с CVE в mongodb. Всё будет сохранятся корректно (сохраняет только json-файлы с CVE).
Дальше буду делать пулл в существующую базу данных, а после авто-пулл.

Upd 3:
Теперь добавление (не обновление!) базы данных делается через ссылку на ZIP-файл. Никаких гитов и репозиториев. Сделал соответствующую валидацию принимаемой на вход ссылки с применением pydantic. Добавить можно только ссылку с MIME-типами, которые показывают, что ссылка направлена на zip. Код скачивает zip-файл во временную директорию, в ней же разархивирует его, закидывает в монгу и стирает из директории. Добавлены обработчики событий. Протестировано с официальной зипкой.
```json
{
  "status": true,
  "message": "Файлы успешно добавлены!",
  "statistics": {
    "Всего файлов": 314943,
    "Успешных обработок": 314943,
    "Ошибок": 0
  }
}
```
Upd 4: сделана ручка обновления, но работает некорректно. Изменена ручка скачивания базы и в целом есть небольшие изменения по всему коду. Ведутся тестирование с тестовым репозиториям. В финише будет проведено обновление базы данных через ручку с официальным файлом.
Upd 4.1: устранил наиглупейшую ошибку, на поиск которой ушло несколько часов. Всё работает корректно.
Upd 5:
разделил проект и вынес все хранящиеся данные в папку database. Настроил импорт из этой папки через симлинки, но как я понял, после транспортировки куда-либо они преобразуются в обычные .py файлы. В любом случае хоть и колхоз вышел, но рабочий. В будущем настрою импорты через Path для надежности. Сделал установщик. Работает без интернета, разве что необходимые для работы образы нужно подгрузить заранее. Обновлен раздел НАСТРОЙКИ.