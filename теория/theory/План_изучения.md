-Pydantic (работа с данными);
-Базы данных и ORM (SQLAlchemy + Alembic). Postgres, MYSQL и др.
-MongoDB;
-Кэширование (FastAPI Cache) Redis;
-Асинхронные задачи Celery RabbitMQ;
-Тестирование Pytest httpx;
-деплой Docker, Docker Compose;
